general:
  zksync_os_version: "0.32.0"
  # SNARK VK of the v32.0 / proving-V8 lane, generated with
  #   zkos-wrapper generate-vk --bin multiblock_batch.bin --text multiblock_batch.text \
  #     --check-aux-params --trusted-setup setup_compact.key
  # and registered on the chain's ZKsyncOSDualVerifier at verifier version 8 (the version
  # the server encodes in `_proof[0]` for V8 proofs).
  # Regenerated at 100-bit security against multiblock_batch.bin md5
  # 31cb9cb3b42d4a183fb858594eeb8706 (zksync-os v0.4.0 release tag @ 69bc4305).
  verification_key: "0x9f7576b911e7d3f528d49f894208682c81800814db9e3beac7fc3b1c4d626e7a"

era-contracts:
  # dz-v32-public-input (era-contracts#2381 head): #2323 (chain config hash in the batch
  # proof public input, chain-id-less batchOutputHash) + the flat multi-batch fold
  # (keccak of the concatenated per-batch hashes, hashed once - a rolling fold diverges
  # from N == 3 on). The previous pin (7644cc62) was an amended-away sibling of this
  # commit that still carried the rolling fold.
  sha: "8fb7c29a4e3174335c6480b23f57822e054f9d5f"

zksync-os-integration-tests:
  sha: "16c6a83b4f634609958f347c56549ba19bf9df9b"

zksync-os-server:
  sha: "c8cd48d2d9d407978491872b1fd7e1e4b9612897"
