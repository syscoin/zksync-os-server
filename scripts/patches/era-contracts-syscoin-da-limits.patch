diff --git a/da-contracts/contracts/CalldataDA.sol b/da-contracts/contracts/CalldataDA.sol
--- a/da-contracts/contracts/CalldataDA.sol
+++ b/da-contracts/contracts/CalldataDA.sol
@@ -7,7 +7,8 @@
 /// @dev Total number of bytes in a blob. Blob = 4096 field elements * 31 bytes per field element
 /// @dev EIP-4844 defines it as 131_072 but we use 4096 * 31 within our circuits to always fit within a field element
 /// @dev Our circuits will prove that a EIP-4844 blob and our internal blob are the same.
-uint256 constant BLOB_SIZE_BYTES = 126_976;
+// SYSCOIN: one Bitcoin DA blob carries up to 2 MiB.
+uint256 constant BLOB_SIZE_BYTES = 2 * 1024 * 1024;
 
 /// @dev The state diff hash, hash of pubdata + the number of blobs.
 uint256 constant BLOB_DATA_OFFSET = 65;
diff --git a/da-contracts/contracts/DAUtils.sol b/da-contracts/contracts/DAUtils.sol
--- a/da-contracts/contracts/DAUtils.sol
+++ b/da-contracts/contracts/DAUtils.sol
@@ -5,7 +5,8 @@
 /// @dev Total number of bytes in a blob. Blob = 4096 field elements * 31 bytes per field element
 /// @dev EIP-4844 defines it as 131_072 but we use 4096 * 31 within our circuits to always fit within a field element
 /// @dev Our circuits will prove that a EIP-4844 blob and our internal blob are the same.
-uint256 constant BLOB_SIZE_BYTES = 126_976;
+// SYSCOIN: one Bitcoin DA blob carries up to 2 MiB.
+uint256 constant BLOB_SIZE_BYTES = 2 * 1024 * 1024;
 
 /// @dev Enum used to determine the source of pubdata. At first we will support calldata and blobs but this can be extended.
 enum PubdataSource {
diff --git a/l1-contracts/contracts/state-transition/chain-interfaces/IExecutor.sol b/l1-contracts/contracts/state-transition/chain-interfaces/IExecutor.sol
--- a/l1-contracts/contracts/state-transition/chain-interfaces/IExecutor.sol
+++ b/l1-contracts/contracts/state-transition/chain-interfaces/IExecutor.sol
@@ -67,7 +67,7 @@
 /// @dev The number of blobs that must be present in the commitment to a batch.
 /// It represents the maximal number of blobs that circuits can support and can be larger
 /// than the maximal number of blobs supported by the contract (`MAX_NUMBER_OF_BLOBS`).
-uint256 constant TOTAL_BLOBS_IN_COMMITMENT = 16;
+uint256 constant TOTAL_BLOBS_IN_COMMITMENT = 32;
 
 /// @title The interface of the ZKsync Executor contract capable of processing events emitted in the ZKsync protocol.
 /// @author Matter Labs
diff --git a/system-contracts/contracts/Constants.sol b/system-contracts/contracts/Constants.sol
--- a/system-contracts/contracts/Constants.sol
+++ b/system-contracts/contracts/Constants.sol
@@ -160,10 +160,10 @@
 /// @dev Total number of bytes in a blob. Blob = 4096 field elements * 31 bytes per field element
 /// @dev EIP-4844 defines it as 131_072 but we use 4096 * 31 within our circuits to always fit within a field element
 /// @dev Our circuits will prove that a EIP-4844 blob and our internal blob are the same.
-uint256 constant BLOB_SIZE_BYTES = 126_976;
+uint256 constant BLOB_SIZE_BYTES = 2 * 1024 * 1024;
 
 /// @dev Max number of blobs currently supported
-uint256 constant MAX_NUMBER_OF_BLOBS = 6;
+uint256 constant MAX_NUMBER_OF_BLOBS = 32;
 
 /// @dev Marker of EraVM bytecode
 uint8 constant ERA_VM_BYTECODE_FLAG = 1;
