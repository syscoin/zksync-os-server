diff --git a/da-contracts/contracts/BlobsL1DAValidatorZKsyncOS.sol b/da-contracts/contracts/BlobsL1DAValidatorZKsyncOS.sol
index 4ee20aa08..a072d7a87 100644
--- a/da-contracts/contracts/BlobsL1DAValidatorZKsyncOS.sol
+++ b/da-contracts/contracts/BlobsL1DAValidatorZKsyncOS.sol
@@ -5,46 +5,24 @@ pragma solidity 0.8.28;
 import {IL1DAValidator, L1DAValidatorOutput} from "./IL1DAValidator.sol";
 
 import {
-    NonEmptyBlobVersionHash,
-    InvalidBlobsDAInputLength,
-    BlobNotPublished,
+    BitcoinDAPrecompileCallFailed,
+    BitcoinDAVerificationFailed,
     InvalidBlobsPublished,
-    EmptyBlobVersionHash
+    InvalidBlobsDAInputLength
 } from "./DAContractsErrors.sol";
 
-/// @dev The number of blocks within each we allow blob to be used for DA.
-/// On Ethereum blobs expire within 4096 epochs, i.e. 4096 * 32 blocks. We reserve
-/// half of the time in order to ensure reader's ability to read the blob's content.
-uint256 constant BLOB_EXPIRATION_BLOCKS = (4096 * 32) / 2;
-
-/// @dev The size in bytes of the versioned hash for a single blob.
-uint256 constant BLOB_VERSIONED_HASH_SIZE = 32;
-
+// SYSCOIN: compact DA validator for ZKsync OS "blob" commitments on non-EIP-4844 L1.
+/// @title L1 DA validator for ZKsync OS “blobs” commitment on non–EIP-4844 L1 (e.g. Syscoin NEVM).
+/// @dev No `blobhash` opcode and no `publishBlobs` / prepublish flow. The operator passes the full list of
+/// 32-byte DA commitment hashes in `operatorDAInput`; each is checked with the Syscoin Bitcoin DA precompile,
+/// then `keccak256(operatorDAInput)` must match the L2 DA commitment (same encoding as ZKsync OS blob mode).
 contract BlobsL1DAValidatorZKsyncOS is IL1DAValidator {
-    /// @notice The published blob versioned hashes.
-    mapping(bytes32 versionedHash => uint256 blockOfPublishing) public publishedBlobs;
+    /// @dev Same Bitcoin DA precompile as `RollupL1DAValidator` on this fork.
+    address internal constant BITCOINDA_PRECOMPILE_ADDR = address(0x63);
+    uint16 internal constant BITCOINDA_PRECOMPILE_COST = 1400;
+    uint256 internal constant SYSCOIN_DA_MAX_BLOBS_PER_BATCH = 32;
 
-    /// @notice Publishes all the blobs provided with a transaction.
-    function publishBlobs() external {
-        uint256 versionedHashIndex = 0;
-        // iterate through all the published blobs in the tx (until versionedHash for index equals to 0)
-        while (true) {
-            bytes32 versionedHash = _getBlobVersionedHash(versionedHashIndex);
-            if (versionedHash == bytes32(0)) {
-                break;
-            }
-            publishedBlobs[versionedHash] = block.number;
-            ++versionedHashIndex;
-        }
-    }
-
-    function isBlobAvailable(bytes32 _versionedHash) public view returns (bool) {
-        uint256 blockOfPublishing = publishedBlobs[_versionedHash];
-
-        // While `block.number` on all used L1 networks is much higher than `BLOB_EXPIRATION_BLOCKS`,
-        // we still check that `blockOfPublishing > 0` just in case.
-        return blockOfPublishing > 0 && block.number - blockOfPublishing <= BLOB_EXPIRATION_BLOCKS;
-    }
+    uint256 private constant WORD = 32;
 
     /// @inheritdoc IL1DAValidator
     function checkDA(
@@ -54,61 +32,37 @@ contract BlobsL1DAValidatorZKsyncOS is IL1DAValidator {
         bytes calldata _operatorDAInput,
         uint256 // _maxBlobsSupported
     ) external view returns (L1DAValidatorOutput memory output) {
-        // As DA input operator should provide concatenated blob versioned hashes of published blobs.
-        // If provided hash equals to 0, blob versioned hash will be taken from blob published with the current transaction.
-        if (_operatorDAInput.length % BLOB_VERSIONED_HASH_SIZE != 0) {
-            revert InvalidBlobsDAInputLength(_operatorDAInput.length);
+        uint256 len = _operatorDAInput.length;
+        if (len == 0 || len % WORD != 0 || len / WORD > SYSCOIN_DA_MAX_BLOBS_PER_BATCH) {
+            revert InvalidBlobsDAInputLength(len);
         }
 
-        uint256 blobsProvided = _operatorDAInput.length / BLOB_VERSIONED_HASH_SIZE;
-
-        bytes memory publishedVersionedHashes = new bytes(_operatorDAInput.length);
-        uint256 versionedHashIndex = 0;
-
-        for (uint256 index = 0; index < blobsProvided; ++index) {
-            bytes32 versionedHash = bytes32(
-                _operatorDAInput[BLOB_VERSIONED_HASH_SIZE * index:BLOB_VERSIONED_HASH_SIZE * (index + 1)]
-            );
-            if (versionedHash != bytes32(0)) {
-                if (!isBlobAvailable(versionedHash)) {
-                    revert BlobNotPublished();
-                }
-            } else {
-                versionedHash = _getBlobVersionedHash(versionedHashIndex);
-                require(versionedHash != bytes32(0), EmptyBlobVersionHash(versionedHashIndex));
-                ++versionedHashIndex;
-            }
-            // efficiently write versionedHash into the byte array
-            assembly {
-                // offset relative to the `publishedVersionedHashes` start
-                // we add 1 to `index` to skip length slot
-                let relativeOffset := mul(add(index, 1), 32)
-                mstore(add(publishedVersionedHashes, relativeOffset), versionedHash)
-            }
-        }
-
-        // This check is required because we want to ensure that there aren't any extra blobs trying to be published.
-        // Calling the BLOBHASH opcode with an index > # blobs - 1 yields bytes32(0)
-        bytes32 nextVersionedHash = _getBlobVersionedHash(versionedHashIndex);
-        if (nextVersionedHash != bytes32(0)) {
-            revert NonEmptyBlobVersionHash(versionedHashIndex);
+        for (uint256 i = 0; i < len; i += WORD) {
+            // SYSCOIN: each 32-byte compact ref must already exist in Bitcoin DA.
+            _verifyBitcoinDA(bytes32(_operatorDAInput[i:i + WORD]));
         }
 
-        // verify that published versionedHashes corresponds to what was generated by ZKsync OS for a batch
-        bytes32 publishedVersionedHashesHash = keccak256(publishedVersionedHashes);
-        if (publishedVersionedHashesHash != _l2DAValidatorOutputHash) {
-            revert InvalidBlobsPublished(publishedVersionedHashesHash, _l2DAValidatorOutputHash);
+        bytes32 publishedHash = keccak256(_operatorDAInput);
+        if (publishedHash != _l2DAValidatorOutputHash) {
+            revert InvalidBlobsPublished(publishedHash, _l2DAValidatorOutputHash);
         }
 
-        // we use this interface for compatibility, but in fact outputs not needed for ZKsync OS
         output.stateDiffHash = bytes32(0);
         output.blobsLinearHashes = new bytes32[](0);
         output.blobsOpeningCommitments = new bytes32[](0);
     }
 
-    function _getBlobVersionedHash(uint256 _index) internal view virtual returns (bytes32 versionedHash) {
-        assembly {
-            versionedHash := blobhash(_index)
+    /// @dev Same pattern as `RollupL1DAValidator._verifyBitcoinDA`.
+    function _verifyBitcoinDA(bytes32 _dataHash) internal view {
+        (bool success, bytes memory result) = BITCOINDA_PRECOMPILE_ADDR.staticcall{gas: BITCOINDA_PRECOMPILE_COST}(
+            abi.encode(_dataHash)
+        );
+
+        if (!success) {
+            revert BitcoinDAPrecompileCallFailed();
+        }
+        if (result.length < 32 || abi.decode(result, (uint256)) == 0) {
+            revert BitcoinDAVerificationFailed();
         }
     }
 }
diff --git a/da-contracts/contracts/DAContractsErrors.sol b/da-contracts/contracts/DAContractsErrors.sol
index 6768d02e2..8a1fc31f0 100644
--- a/da-contracts/contracts/DAContractsErrors.sol
+++ b/da-contracts/contracts/DAContractsErrors.sol
@@ -51,3 +51,7 @@ error BlobNotPublished();
 
 // 0x8b003a9d
 error InvalidBlobsPublished(bytes32 publishedHash, bytes32 expectedHash);
+
+// Syscoin Bitcoin DA precompile (used by `BlobsL1DAValidatorZKsyncOS` / rollup fork paths).
+error BitcoinDAPrecompileCallFailed();
+error BitcoinDAVerificationFailed();
diff --git a/da-contracts/contracts/RollupL1DAValidator.sol b/da-contracts/contracts/RollupL1DAValidator.sol
index 34163db26..872b17a9e 100644
--- a/da-contracts/contracts/RollupL1DAValidator.sol
+++ b/da-contracts/contracts/RollupL1DAValidator.sol
@@ -218,8 +218,6 @@ contract RollupL1DAValidator is IL1DAValidator, CalldataDA {
     }
 
     function _getBlobVersionedHash(uint256 _index) internal view virtual returns (bytes32 versionedHash) {
-        assembly {
-            versionedHash := blobhash(_index)
-        }
+        versionedHash = 0;
     }
 }
diff --git a/da-contracts/test/foundry/BlobsL1DaValidatorZKsyncOS.t.sol b/da-contracts/test/foundry/BlobsL1DaValidatorZKsyncOS.t.sol
index fff80d4af..5909c7550 100644
--- a/da-contracts/test/foundry/BlobsL1DaValidatorZKsyncOS.t.sol
+++ b/da-contracts/test/foundry/BlobsL1DaValidatorZKsyncOS.t.sol
@@ -2,133 +2,115 @@
 pragma solidity 0.8.28;
 
 import {Test} from "forge-std/Test.sol";
-import {MockBlobsL1DAValidator} from "./MockBlobsL1DAValidator.sol";
-import {BLOB_EXPIRATION_BLOCKS} from "../../contracts/BlobsL1DAValidatorZKsyncOS.sol";
+import {BlobsL1DAValidatorZKsyncOS} from "../../contracts/BlobsL1DAValidatorZKsyncOS.sol";
 import {
+    BitcoinDAPrecompileCallFailed,
+    BitcoinDAVerificationFailed,
     InvalidBlobsPublished,
-    InvalidBlobsDAInputLength,
-    BlobNotPublished,
-    NonEmptyBlobVersionHash
+    InvalidBlobsDAInputLength
 } from "../../contracts/DAContractsErrors.sol";
 import {L1DAValidatorOutput} from "../../contracts/IL1DAValidator.sol";
 
-contract BlobsL1DAValidatorZKsyncOSTest is Test {
-    MockBlobsL1DAValidator internal validator;
-
-    function setUp() public {
-        validator = new MockBlobsL1DAValidator();
+/// @dev Etched at `0x63` so `staticcall` succeeds with non-empty return data.
+contract BitcoinDAMockOk {
+    fallback() external {
+        assembly {
+            mstore(0, 1)
+            return(0, 32)
+        }
     }
+}
 
-    function testPublishBlobsStoresHashes() public {
-        bytes32[] memory blobs = new bytes32[](2);
-        blobs[0] = keccak256("blob1");
-        blobs[1] = keccak256("blob2");
-        validator.setMockBlobs(blobs);
-
-        validator.publishBlobs();
+/// @dev Etched at `0x63` to simulate DA lookup failure.
+contract BitcoinDAMockFail {
+    fallback() external {
+        revert();
+    }
+}
 
-        assertEq(validator.publishedBlobs(blobs[0]), block.number);
-        assertEq(validator.publishedBlobs(blobs[1]), block.number);
-        assertEq(validator.publishedBlobs(keccak256("nonexistent")), 0);
+/// @dev Etched at `0x63`: call succeeds but returns no data.
+contract BitcoinDAMockEmptyReturn {
+    fallback() external {
+        assembly {
+            return(0, 0)
+        }
     }
+}
 
-    function testIsBlobAvailable() public {
-        bytes32 blobHash = keccak256("blob");
-        bytes32[] memory blobs = new bytes32[](1);
-        blobs[0] = blobHash;
-        validator.setMockBlobs(blobs);
+/// @dev Etched at `0x63`: call succeeds but reports DA as unavailable.
+contract BitcoinDAMockZeroReturn {
+    fallback() external {
+        assembly {
+            mstore(0, 0)
+            return(0, 32)
+        }
+    }
+}
 
-        validator.publishBlobs();
-        assertTrue(validator.isBlobAvailable(blobHash));
+contract BlobsL1DAValidatorZKsyncOSTest is Test {
+    BlobsL1DAValidatorZKsyncOS internal validator;
 
-        // Move far into the future past expiration
-        vm.roll(block.number + BLOB_EXPIRATION_BLOCKS + 1);
-        assertFalse(validator.isBlobAvailable(blobHash));
+    function setUp() public {
+        validator = new BlobsL1DAValidatorZKsyncOS();
+        vm.etch(address(uint160(0x63)), address(new BitcoinDAMockOk()).code);
     }
 
     function testCheckDAInvalidLength() public {
-        bytes memory badInput = hex"1234"; // not multiple of 32
+        bytes memory badInput = hex"1234";
         vm.expectRevert(abi.encodeWithSelector(InvalidBlobsDAInputLength.selector, 2));
-        // solhint-disable-next-line func-named-parameters
         validator.checkDA(1, 1, bytes32(0), badInput, 0);
     }
 
-    function testCheckDABlobNotPublished() public {
-        bytes32 notPublished = keccak256("ghost");
-        bytes memory operatorInput = abi.encodePacked(notPublished);
-
-        vm.expectRevert(BlobNotPublished.selector);
-        // solhint-disable-next-line func-named-parameters
-        validator.checkDA(1, 1, keccak256(operatorInput), operatorInput, 0);
-    }
-
-    function testCheckDANonEmptyBlobVersionHash() public {
-        bytes32[] memory blobs = new bytes32[](2);
-        blobs[0] = keccak256("b1");
-        blobs[1] = keccak256("b2");
-        validator.setMockBlobs(blobs);
-
-        // one zero in operator input -> will pull blob0, but blob1 is still non-empty
-        bytes memory operatorInput = abi.encodePacked(bytes32(0));
-
-        vm.expectRevert(abi.encodeWithSelector(NonEmptyBlobVersionHash.selector, 1));
-        // solhint-disable-next-line func-named-parameters
-        validator.checkDA(1, 1, keccak256(operatorInput), operatorInput, 0);
+    function testCheckDAEmptyInput() public {
+        vm.expectRevert(abi.encodeWithSelector(InvalidBlobsDAInputLength.selector, 0));
+        validator.checkDA(1, 1, bytes32(0), "", 0);
     }
 
     function testCheckDAInvalidBlobsPublished() public {
-        bytes32[] memory blobs = new bytes32[](1);
-        blobs[0] = keccak256("blob");
-        validator.setMockBlobs(blobs);
-
-        // ask to pull blob0
-        bytes memory operatorInput = abi.encodePacked(bytes32(0));
-
-        bytes32 wrongHash = keccak256("wrong");
-        vm.expectRevert(
-            abi.encodeWithSelector(InvalidBlobsPublished.selector, keccak256(abi.encodePacked(blobs[0])), wrongHash)
-        );
-        // solhint-disable-next-line func-named-parameters
-        validator.checkDA(1, 1, wrongHash, operatorInput, 0);
+        bytes32 h0 = keccak256("a");
+        bytes32 h1 = keccak256("b");
+        bytes memory operatorInput = abi.encodePacked(h0, h1);
+        bytes32 wrong = keccak256("wrong");
+        vm.expectRevert(abi.encodeWithSelector(InvalidBlobsPublished.selector, keccak256(operatorInput), wrong));
+        validator.checkDA(1, 1, wrong, operatorInput, 0);
     }
 
     function testCheckDAVerifiesCorrectly() public {
-        bytes32[] memory blobs = new bytes32[](1);
-        blobs[0] = keccak256("blob");
-        validator.setMockBlobs(blobs);
+        bytes32 h0 = keccak256("a");
+        bytes32 h1 = keccak256("b");
+        bytes memory operatorInput = abi.encodePacked(h0, h1);
+        bytes32 expectedHash = keccak256(operatorInput);
 
-        // operator input asks to pull blob0
-        bytes memory operatorInput = abi.encodePacked(bytes32(0));
-        bytes32 expectedHash = keccak256(abi.encodePacked(blobs[0]));
-
-        // solhint-disable-next-line func-named-parameters
         L1DAValidatorOutput memory out = validator.checkDA(1, 1, expectedHash, operatorInput, 0);
 
         assertEq(out.stateDiffHash, bytes32(0));
         assertEq(out.blobsLinearHashes.length, 0);
     }
 
-    function testCheckDAWithMixedProvidedAndPublishedBlob() public {
-        // publish one blob beforehand
-        bytes32 publishedBlob = keccak256("published");
-        bytes32[] memory blobs = new bytes32[](1);
-        blobs[0] = publishedBlob;
-        validator.setMockBlobs(blobs);
-        validator.publishBlobs();
+    function testCheckDABitcoinDAPrecompileFails() public {
+        vm.etch(address(uint160(0x63)), address(new BitcoinDAMockFail()).code);
+
+        bytes32 h0 = keccak256("a");
+        bytes memory operatorInput = abi.encodePacked(h0);
+        vm.expectRevert(BitcoinDAPrecompileCallFailed.selector);
+        validator.checkDA(1, 1, keccak256(operatorInput), operatorInput, 0);
+    }
 
-        // operator provides one directly + one placeholder for pulling from tx (mock)
-        bytes32 providedBlob = keccak256("provided");
-        bytes32[] memory txBlobs = new bytes32[](1);
-        txBlobs[0] = providedBlob;
-        validator.setMockBlobs(txBlobs); // will be pulled for the zero placeholder
+    function testCheckDABitcoinDAEmptyResult() public {
+        vm.etch(address(uint160(0x63)), address(new BitcoinDAMockEmptyReturn()).code);
 
-        bytes memory operatorInput = abi.encodePacked(publishedBlob, bytes32(0));
-        bytes32 expectedHash = keccak256(abi.encodePacked(publishedBlob, providedBlob));
+        bytes32 h0 = keccak256("a");
+        bytes memory operatorInput = abi.encodePacked(h0);
+        vm.expectRevert(BitcoinDAVerificationFailed.selector);
+        validator.checkDA(1, 1, keccak256(operatorInput), operatorInput, 0);
+    }
 
-        // solhint-disable-next-line func-named-parameters
-        L1DAValidatorOutput memory out = validator.checkDA(1, 1, expectedHash, operatorInput, 0);
+    function testCheckDABitcoinDAZeroResult() public {
+        vm.etch(address(uint160(0x63)), address(new BitcoinDAMockZeroReturn()).code);
 
-        assertEq(out.stateDiffHash, bytes32(0));
-        assertEq(out.blobsLinearHashes.length, 0);
+        bytes32 h0 = keccak256("a");
+        bytes memory operatorInput = abi.encodePacked(h0);
+        vm.expectRevert(BitcoinDAVerificationFailed.selector);
+        validator.checkDA(1, 1, keccak256(operatorInput), operatorInput, 0);
     }
 }
diff --git a/da-contracts/test/foundry/MockBlobsL1DAValidator.sol b/da-contracts/test/foundry/MockBlobsL1DAValidator.sol
index 9205164f8..878415fdc 100644
--- a/da-contracts/test/foundry/MockBlobsL1DAValidator.sol
+++ b/da-contracts/test/foundry/MockBlobsL1DAValidator.sol
@@ -3,22 +3,9 @@ pragma solidity 0.8.28;
 
 import {BlobsL1DAValidatorZKsyncOS} from "../../contracts/BlobsL1DAValidatorZKsyncOS.sol";
 
-/// @dev Mock contract to override _getBlobVersionedHash
+/// @dev Backward-compatible test helper retained for import stability.
 contract MockBlobsL1DAValidator is BlobsL1DAValidatorZKsyncOS {
-    bytes32[] internal mockBlobs;
-
-    function setMockBlobs(bytes32[] calldata blobs) external {
-        delete mockBlobs;
-        uint256 len = blobs.length;
-        for (uint256 i = 0; i < len; ++i) {
-            mockBlobs.push(blobs[i]);
-        }
-    }
-
-    function _getBlobVersionedHash(uint256 _index) internal view override returns (bytes32) {
-        if (_index < mockBlobs.length) {
-            return mockBlobs[_index];
-        }
-        return bytes32(0);
+    // No-op in Syscoin Bitcoin DA mode: blobhash prepublish path is removed.
+    function setMockBlobs(bytes32[] calldata) external pure {
     }
 }
diff --git a/l1-contracts/contracts/bridge/BridgeHelper.sol b/l1-contracts/contracts/bridge/BridgeHelper.sol
index 2fc7fee09..2d886aba6 100644
--- a/l1-contracts/contracts/bridge/BridgeHelper.sol
+++ b/l1-contracts/contracts/bridge/BridgeHelper.sol
@@ -22,9 +22,10 @@ library BridgeHelper {
         bytes memory symbol;
         bytes memory decimals;
         if (_token == ETH_TOKEN_ADDRESS) {
-            // when depositing eth to a non-eth based chain it is an ERC20
-            name = abi.encode("Ether");
-            symbol = abi.encode("ETH");
+            // SYSCOIN
+            // when depositing SYS to a non-SYS based chain it is an ERC20
+            name = abi.encode("Syscoin");
+            symbol = abi.encode("SYS");
             decimals = abi.encode(uint8(18));
         } else {
             bool success;
diff --git a/l1-contracts/contracts/upgrades/L1FixedForceDeploymentsHelper.sol b/l1-contracts/contracts/upgrades/L1FixedForceDeploymentsHelper.sol
index cf757a7d2..0ba52b79a 100644
--- a/l1-contracts/contracts/upgrades/L1FixedForceDeploymentsHelper.sol
+++ b/l1-contracts/contracts/upgrades/L1FixedForceDeploymentsHelper.sol
@@ -45,8 +45,8 @@ abstract contract L1FixedForceDeploymentsHelper {
         // It is required for a base token to implement the following methods
         TokenMetadata memory tokenData;
         if (_baseTokenAddress == ETH_TOKEN_ADDRESS) {
-            tokenData = TokenMetadata({name: string("Ether"), symbol: string("ETH"), decimals: 18});
+            // SYSCOIN: address(1) is the native SYS sentinel in this deployment.
+            tokenData = TokenMetadata({name: string("Syscoin"), symbol: string("SYS"), decimals: 18});
         } else {
             (string memory stringResult, bool success) = _safeCallTokenMetadataString(
                 _baseTokenAddress,
diff --git a/l1-contracts/contracts/state-transition/chain-deps/facets/Committer.sol b/l1-contracts/contracts/state-transition/chain-deps/facets/Committer.sol
index f5406260e..58895d3b2 100644
--- a/l1-contracts/contracts/state-transition/chain-deps/facets/Committer.sol
+++ b/l1-contracts/contracts/state-transition/chain-deps/facets/Committer.sol
@@ -85,6 +85,19 @@ uint8 constant RELAYED_EXECUTOR_VERSION = 0;
 /// @dev The version that is used for the `Executor` calldata used for relaying the
 /// ZKSync OS stored batch info.
 uint8 constant RELAYED_EXECUTOR_VERSION_ZKSYNC_OS = 1;
+// SYSCOIN: compact edge DA refs emitted by Gateway child-chain commits and verified on final L1.
+uint8 constant SYSCOIN_RELAYED_EDGE_DA_VALIDATOR_VERSION = 1;
+bytes constant SYSCOIN_EDGE_DA_REFS_DOMAIN = "SYSCOIN_EDGE_DA_REFS_V1";
+address constant SYSCOIN_BITCOINDA_PRECOMPILE_ADDR = address(0x63);
+uint16 constant SYSCOIN_BITCOINDA_PRECOMPILE_COST = 1400;
+uint256 constant SYSCOIN_EDGE_DA_REF_HEAD_BYTES = 32 * 5;
+uint256 constant SYSCOIN_DA_MAX_BLOBS_PER_BATCH = 32;
+
+// SYSCOIN: final L1 settlement must exactly open the Gateway-proven edge DA refs root.
+error SyscoinEdgeDARefsRootMismatch(bytes32 expectedRoot, bytes32 actualRoot);
+error SyscoinEdgeDARefInputMalformed(uint256 offset);
+error SyscoinEdgeDAPrecompileCallFailed(bytes32 blobVersionHash);
+error SyscoinEdgeDANotAvailable(bytes32 blobVersionHash);
 
 /// @title ZK chain Committer contract responsible for batch commitment operations.
 /// @author Matter Labs
@@ -405,6 +418,99 @@ contract CommitterFacet is ZKChainBase, ICommitter {
         }
     }
 
+    function _verifySyscoinBitcoinDA(bytes32 _blobVersionHash) internal view {
+        // SYSCOIN: this mirrors the Syscoin DA validator check used for Gateway's own DA.
+        (bool success, bytes memory result) = SYSCOIN_BITCOINDA_PRECOMPILE_ADDR.staticcall{
+            gas: SYSCOIN_BITCOINDA_PRECOMPILE_COST
+        }(abi.encode(_blobVersionHash));
+
+        if (!success) {
+            revert SyscoinEdgeDAPrecompileCallFailed(_blobVersionHash);
+        }
+        if (result.length == 0 || abi.decode(result, (uint256)) == 0) {
+            revert SyscoinEdgeDANotAvailable(_blobVersionHash);
+        }
+    }
+
+    function _readSyscoinEdgeDARefWord(bytes memory _edgeDARefsInput, uint256 _offset) internal pure returns (uint256) {
+        (bytes32 word, ) = UnsafeBytes.readBytes32(_edgeDARefsInput, _offset);
+        return uint256(word);
+    }
+
+    function _copySyscoinBytes(
+        bytes memory _dest,
+        uint256 _destOffset,
+        bytes memory _src,
+        uint256 _srcOffset,
+        uint256 _length
+    ) internal pure {
+        assembly ("memory-safe") {
+            mcopy(add(add(_dest, 0x20), _destOffset), add(add(_src, 0x20), _srcOffset), _length)
+        }
+    }
+
+    function _syscoinEdgeDARefHash(
+        bytes memory _edgeDARefsInput,
+        uint256 _offset,
+        uint256 _blobHashesStart,
+        uint256 _blobHashesLength
+    ) internal pure returns (bytes32) {
+        bytes memory edgeRefPreimage = new bytes(SYSCOIN_EDGE_DA_REFS_DOMAIN.length + 32 * 4 + _blobHashesLength);
+        uint256 preimageOffset;
+        _copySyscoinBytes(
+            edgeRefPreimage,
+            preimageOffset,
+            SYSCOIN_EDGE_DA_REFS_DOMAIN,
+            0,
+            SYSCOIN_EDGE_DA_REFS_DOMAIN.length
+        );
+        preimageOffset += SYSCOIN_EDGE_DA_REFS_DOMAIN.length;
+        _copySyscoinBytes(edgeRefPreimage, preimageOffset, _edgeDARefsInput, _offset + 32, 32 * 3);
+        preimageOffset += 32 * 3;
+        assembly {
+            mstore(add(add(edgeRefPreimage, 0x20), preimageOffset), div(_blobHashesLength, 32))
+        }
+        preimageOffset += 32;
+        _copySyscoinBytes(edgeRefPreimage, preimageOffset, _edgeDARefsInput, _blobHashesStart, _blobHashesLength);
+        return keccak256(edgeRefPreimage);
+    }
+
+    function _verifySyscoinEdgeDARefs(bytes memory _edgeDARefsInput, bytes32 _expectedRoot) internal view {
+        // SYSCOIN: ordered rolling root over compact edge DA ref messages emitted on Gateway.
+        bytes32 root;
+        uint256 offset;
+        uint256 inputLength = _edgeDARefsInput.length;
+
+        while (offset < inputLength) {
+            if (inputLength - offset < SYSCOIN_EDGE_DA_REF_HEAD_BYTES + 32) {
+                revert SyscoinEdgeDARefInputMalformed(offset);
+            }
+
+            uint256 version = _readSyscoinEdgeDARefWord(_edgeDARefsInput, offset);
+            uint256 blobHashesOffset = _readSyscoinEdgeDARefWord(_edgeDARefsInput, offset + 128);
+            uint256 blobHashesLength = _readSyscoinEdgeDARefWord(_edgeDARefsInput, offset + 160);
+
+            if (
+                version != SYSCOIN_RELAYED_EDGE_DA_VALIDATOR_VERSION ||
+                blobHashesOffset != SYSCOIN_EDGE_DA_REF_HEAD_BYTES ||
+                blobHashesLength == 0 ||
+                blobHashesLength % 32 != 0 ||
+                blobHashesLength / 32 > SYSCOIN_DA_MAX_BLOBS_PER_BATCH
+            ) {
+                revert SyscoinEdgeDARefInputMalformed(offset);
+            }
+
+            uint256 blobHashesStart = offset + SYSCOIN_EDGE_DA_REF_HEAD_BYTES + 32;
+            uint256 blobHashesEnd = blobHashesStart + blobHashesLength;
+            uint256 paddedBlobHashesEnd = blobHashesStart + ((blobHashesLength + 31) / 32) * 32;
+            if (paddedBlobHashesEnd > inputLength) {
+                revert SyscoinEdgeDARefInputMalformed(offset);
+            }
+
+            bytes32 edgeRefHash = _syscoinEdgeDARefHash(_edgeDARefsInput, offset, blobHashesStart, blobHashesLength);
+            root = keccak256(abi.encodePacked(root, edgeRefHash));
+
+            for (uint256 hashOffset = blobHashesStart; hashOffset < blobHashesEnd; hashOffset += 32) {
+                (bytes32 blobVersionHash, ) = UnsafeBytes.readBytes32(_edgeDARefsInput, hashOffset);
+                _verifySyscoinBitcoinDA(blobVersionHash);
+            }
+
+            offset = paddedBlobHashesEnd;
+        }
+
+        if (root != _expectedRoot) {
+            revert SyscoinEdgeDARefsRootMismatch(_expectedRoot, root);
+        }
+    }
+
     function _commitOneBatchZKsyncOS(
         IExecutor.StoredBatchInfo memory _previousBatch,
         CommitBatchInfoZKsyncOS memory _newBatch,
@@ -477,6 +583,9 @@ contract CommitterFacet is ZKChainBase, ICommitter {
         if (_newBatch.slChainId != block.chainid) {
             revert SettlementLayerChainIdMismatch();
         }
+        // SYSCOIN: always verify the Gateway-proven compact edge DA refs opening.
+        // Empty input/root passes for batches with no edge DA refs; non-empty roots must open to Bitcoin DA.
+        _verifySyscoinEdgeDARefs(_newBatch.edgeDARefsInput, _newBatch.edgeDARefsRoot);
 
         // The batch proof public input can be calculated as keccak256(state_commitment_before & state_commitment_after & batch_output_hash)
         // batch output hash commits to information about batch that needs to be opened on l1.
@@ -494,7 +603,9 @@ contract CommitterFacet is ZKChainBase, ICommitter {
                 _newBatch.l2LogsTreeRoot,
                 _expectedSystemContractUpgradeTxHash,
                 _newBatch.dependencyRootsRollingHash,
-                _newBatch.slChainId
+                _newBatch.slChainId,
+                // SYSCOIN: bind compact edge DA refs into the proven Gateway batch output.
+                _newBatch.edgeDARefsRoot
             )
         );
 
diff --git a/l1-contracts/contracts/state-transition/chain-deps/gateway-ctm-deployer/GatewayCTMDeployerDA.sol b/l1-contracts/contracts/state-transition/chain-deps/gateway-ctm-deployer/GatewayCTMDeployerDA.sol
index 55ce6a6cc..19b7f8aa6 100644
--- a/l1-contracts/contracts/state-transition/chain-deps/gateway-ctm-deployer/GatewayCTMDeployerDA.sol
+++ b/l1-contracts/contracts/state-transition/chain-deps/gateway-ctm-deployer/GatewayCTMDeployerDA.sol
@@ -6,7 +6,7 @@ import {RollupDAManager} from "../../data-availability/RollupDAManager.sol";
 import {RelayedSLDAValidator} from "../../data-availability/RelayedSLDAValidator.sol";
 import {ValidiumL1DAValidator} from "../../data-availability/ValidiumL1DAValidator.sol";
 
-import {ROLLUP_L2_DA_COMMITMENT_SCHEME} from "../../../common/Config.sol";
+import {L2DACommitmentScheme} from "../../../common/Config.sol";
 
 import {DAContracts} from "contracts/common/StateTransitionTypes.sol";
 import {GatewayDADeployerConfig} from "./GatewayCTMDeployer.sol";
@@ -36,7 +36,9 @@ contract GatewayCTMDeployerDA {
         result.rollupSLDAValidator = address(new RelayedSLDAValidator{salt: salt}());
 
         // Initialize DA manager
-        rollupDAManager.updateDAPair(result.rollupSLDAValidator, ROLLUP_L2_DA_COMMITMENT_SCHEME, true);
+        // SYSCOIN: Gateway child chains use compact Bitcoin DA refs with the
+        // ZKsync OS commitment scheme, not the upstream Era rollup scheme.
+        rollupDAManager.updateDAPair(result.rollupSLDAValidator, L2DACommitmentScheme.BLOBS_ZKSYNC_OS, true);
         // Note, that the governance still has to accept it.
         // It will happen in a separate voting after the deployment is done.
         rollupDAManager.transferOwnership(_config.aliasedGovernanceAddress);
diff --git a/l1-contracts/contracts/state-transition/chain-interfaces/ICommitter.sol b/l1-contracts/contracts/state-transition/chain-interfaces/ICommitter.sol
index d282201b8..268a502a1 100644
--- a/l1-contracts/contracts/state-transition/chain-interfaces/ICommitter.sol
+++ b/l1-contracts/contracts/state-transition/chain-interfaces/ICommitter.sol
@@ -48,6 +48,8 @@ struct CommitBatchInfo {
 /// @param l2LogsTreeRoot Root hash of tree that contains L2 -> L1 messages from this batch
 /// @param daCommitmentScheme commitment scheme used to generate pubdata commitment for this batch
 /// @param daCommitment commitment to the batch pubdata to validate DA in the l1 da validator
+/// @param edgeDARefsInput compact edge DA ref messages used as the final-L1 root opening
+/// @param edgeDARefsRoot root of compact edge DA refs emitted by chains settling to Gateway
 // solhint-disable-next-line gas-struct-packing
 struct CommitBatchInfoZKsyncOS {
     uint64 batchNumber;
@@ -65,6 +67,10 @@ struct CommitBatchInfoZKsyncOS {
     uint64 lastBlockNumber;
     uint256 chainId;
     bytes operatorDAInput;
+    // SYSCOIN: compact edge DA ref messages used as the final-L1 root opening.
+    bytes edgeDARefsInput;
+    // SYSCOIN: root of compact edge DA refs emitted by chains settling to Gateway.
+    bytes32 edgeDARefsRoot;
     uint256 slChainId;
 }
 
diff --git a/l1-contracts/contracts/state-transition/data-availability/RelayedSLDAValidator.sol b/l1-contracts/contracts/state-transition/data-availability/RelayedSLDAValidator.sol
index 232920082..4c9430d3c 100644
--- a/l1-contracts/contracts/state-transition/data-availability/RelayedSLDAValidator.sol
+++ b/l1-contracts/contracts/state-transition/data-availability/RelayedSLDAValidator.sol
@@ -2,20 +2,21 @@
 
 pragma solidity 0.8.28;
 
-import {IL1DAValidator, L1DAValidatorOutput, PubdataSource} from "../chain-interfaces/IL1DAValidator.sol";
+import {IL1DAValidator, L1DAValidatorOutput} from "../chain-interfaces/IL1DAValidator.sol";
 import {CalldataDAGateway} from "./CalldataDAGateway.sol";
 
 import {IL2Bridgehub} from "../../core/bridgehub/IL2Bridgehub.sol";
 import {L2_BRIDGEHUB_ADDR, L2_TO_L1_MESSENGER_SYSTEM_CONTRACT} from "../../common/l2-helpers/L2ContractInterfaces.sol";
-import {
-    BlobHashBlobCommitmentMismatchValue,
-    InvalidPubdataSource,
-    L1DAValidatorInvalidSender
-} from "../L1StateTransitionErrors.sol";
+import {L1DAValidatorInvalidSender} from "../L1StateTransitionErrors.sol";
 
 /// @dev The version that is used for the `RelayedSLDAValidator` calldata.
 /// This is needed to ensure easier future-compatible encoding.
 uint8 constant RELAYED_SL_DA_VALIDATOR_VERSION = 0;
+// SYSCOIN: compact edge DA refs emitted to Gateway instead of relaying full edge-chain pubdata.
+uint8 constant SYSCOIN_RELAYED_EDGE_DA_VALIDATOR_VERSION = 1;
+uint256 constant SYSCOIN_DA_MAX_BLOBS_PER_BATCH = 32;
+error SyscoinEdgeDARefInputMalformed();
+error SyscoinEdgeDACommitmentMismatch(bytes32 expectedCommitment, bytes32 actualCommitment);
 
 /// @notice The DA validator intended to be used in Era-environment.
 /// @dev For compatibility reasons it accepts calldata in the same format as the `RollupL1DAValidator`, but unlike the latter it
@@ -44,6 +45,19 @@ contract RelayedSLDAValidator is IL1DAValidator, CalldataDAGateway {
         );
     }
 
+    function _relaySyscoinEdgeDARef(
+        uint256 _chainId,
+        uint256 _batchNumber,
+        bytes32 _daCommitment,
+        bytes calldata _blobVersionHashes
+    ) internal {
+        // SYSCOIN: zksys publishes pubdata directly to Bitcoin DA; Gateway only relays compact refs.
+        // slither-disable-next-line unused-return
+        L2_TO_L1_MESSENGER_SYSTEM_CONTRACT.sendToL1(
+            abi.encode(SYSCOIN_RELAYED_EDGE_DA_VALIDATOR_VERSION, _chainId, _batchNumber, _daCommitment, _blobVersionHashes)
+        );
+    }
+
     /// @inheritdoc IL1DAValidator
     function checkDA(
         uint256 _chainId,
@@ -56,56 +70,22 @@ contract RelayedSLDAValidator is IL1DAValidator, CalldataDAGateway {
         // because of the stack-too-deep error caused by it.
         _ensureOnlyChainSender(_chainId);
 
-        // Preventing "stack too deep" error
-        uint256 blobsProvided;
-        bytes32 fullPubdataHash;
-        bytes calldata l1DaInput;
-        {
-            bytes32 stateDiffHash;
-            bytes32[] memory blobsLinearHashes;
-            (
-                stateDiffHash,
-                fullPubdataHash,
-                blobsLinearHashes,
-                blobsProvided,
-                l1DaInput
-            ) = _processL2RollupDAValidatorOutputHash(_l2DAValidatorOutputHash, _maxBlobsSupported, _operatorDAInput);
-
-            output.stateDiffHash = stateDiffHash;
-            output.blobsLinearHashes = blobsLinearHashes;
+        // SYSCOIN: compact ZKsync OS path. The operator input is the ordered 32-byte Bitcoin DA
+        // hashes, and the OS/Committer commitment is keccak256(operatorDAInput). Fail closed so
+        // malformed compact refs cannot fall through to the legacy full-pubdata relay path.
+        if (
+            _operatorDAInput.length == 0 ||
+            _operatorDAInput.length % 32 != 0 ||
+            _operatorDAInput.length / 32 > SYSCOIN_DA_MAX_BLOBS_PER_BATCH
+        ) {
+            revert SyscoinEdgeDARefInputMalformed();
         }
-
-        uint8 pubdataSource = uint8(l1DaInput[0]);
-
-        // Note, that the blobs are not supported in the RelayedSLDAValidator.
-        if (pubdataSource == uint8(PubdataSource.Calldata)) {
-            bytes calldata pubdata;
-            bytes32[] memory blobCommitments;
-
-            (blobCommitments, pubdata) = _processCalldataDA(
-                blobsProvided,
-                fullPubdataHash,
-                _maxBlobsSupported,
-                l1DaInput[1:]
-            );
-
-            _relayCalldata(_chainId, _batchNumber, pubdata);
-
-            output.blobsOpeningCommitments = blobCommitments;
-        } else {
-            revert InvalidPubdataSource(pubdataSource);
-        }
-
-        // We verify that for each set of blobHash/blobCommitment are either both empty
-        // or there are values for both.
-        // This is mostly a sanity check and it is not strictly required.
-        for (uint256 i = 0; i < _maxBlobsSupported; ++i) {
-            if (
-                (output.blobsLinearHashes[i] != bytes32(0) || output.blobsOpeningCommitments[i] != bytes32(0)) &&
-                (output.blobsLinearHashes[i] == bytes32(0) || output.blobsOpeningCommitments[i] == bytes32(0))
-            ) {
-                revert BlobHashBlobCommitmentMismatchValue();
-            }
+        bytes32 actualCommitment = keccak256(_operatorDAInput);
+        if (actualCommitment != _l2DAValidatorOutputHash) {
+            revert SyscoinEdgeDACommitmentMismatch(_l2DAValidatorOutputHash, actualCommitment);
         }
+        output.blobsLinearHashes = new bytes32[](_maxBlobsSupported);
+        output.blobsOpeningCommitments = new bytes32[](_maxBlobsSupported);
+        _relaySyscoinEdgeDARef(_chainId, _batchNumber, _l2DAValidatorOutputHash, _operatorDAInput);
     }
 }
diff --git a/l1-contracts/contracts/state-transition/verifiers/ZKsyncOSVerifierPlonk.sol b/l1-contracts/contracts/state-transition/verifiers/ZKsyncOSVerifierPlonk.sol
index 199e366fd..110f50a4b 100644
--- a/l1-contracts/contracts/state-transition/verifiers/ZKsyncOSVerifierPlonk.sol
+++ b/l1-contracts/contracts/state-transition/verifiers/ZKsyncOSVerifierPlonk.sol
@@ -9,7 +9,7 @@ import {IVerifier} from "../chain-interfaces/IVerifier.sol";
 /// @notice Modified version of the Permutations over Lagrange-bases for Oecumenical Noninteractive arguments of
 /// Knowledge (PLONK) verifier.
 /// Modifications have been made to optimize the proof system for ZK chain circuits.
-/// @dev Contract was generated from a verification key with a hash of 0x23156cf220288cd1e436dccfc09aa4883ea8288da61aa69e2c7251b0c0c44ccd
+/// @dev Contract was generated from a verification key with a hash of 0x6f837bbef255ebde36677f3accb456e16253fe43f4091b0e820bff0cf95a32a0
 /// @dev It uses a custom memory layout inside the inline assembly block. Each reserved memory cell is declared in the
 /// constants below.
 /// @dev For a better understanding of the verifier algorithm please refer to the following papers:
@@ -284,8 +284,8 @@ contract ZKsyncOSVerifierPlonk is IVerifier {
     function _loadVerificationKey() internal pure virtual {
         assembly {
             // gate setup commitments
-            mstore(VK_GATE_SETUP_0_X_SLOT, 0x110c766cba8f440348864e6180f0db40c8d4d373b0de0c1e217b6cf427da655a)
-            mstore(VK_GATE_SETUP_0_Y_SLOT, 0x0328c0e1946fcfd01b9a0e23d6856b7d9b99b5f3466ccdc6763fb55787f26f49)
+            mstore(VK_GATE_SETUP_0_X_SLOT, 0x293d62a231cae0f4a28ea8925e4ef500b6f6672bac457a9a345dcb0a7b0e80a5)
+            mstore(VK_GATE_SETUP_0_Y_SLOT, 0x1dca07081c781b718a0ccd3ddfb7fc71c950f6a61ddb678a32f0996b3d156d34)
             mstore(VK_GATE_SETUP_1_X_SLOT, 0x0feda978f4639d0a725df74c4738bf178adca34e6a4f7cc38d6a9ae9ee49f695)
             mstore(VK_GATE_SETUP_1_Y_SLOT, 0x2f7068beedb881aea392246c33ccc026ea9c8f6ea558a055b6df00ebfdf210a3)
             mstore(VK_GATE_SETUP_2_X_SLOT, 0x06eccb685da5c8cc09a13698b2a22e383025abc75393d68c1da5a7dbacffbd9b)
@@ -296,8 +296,8 @@ contract ZKsyncOSVerifierPlonk is IVerifier {
             mstore(VK_GATE_SETUP_4_Y_SLOT, 0x1ac2f036e3de6e4a9bacd5eacc778fa3fbdf5fd4b06e551038c704adecbeb639)
             mstore(VK_GATE_SETUP_5_X_SLOT, 0x048e2130f16d54d7e07df66330b4d892753b5141ecacb4007573d9ea4a8ff028)
             mstore(VK_GATE_SETUP_5_Y_SLOT, 0x13a3819d4cb80114e51de741a5fb48ce4ba20e82c5d39d16f70ca0461f49a131)
-            mstore(VK_GATE_SETUP_6_X_SLOT, 0x1c6e4decdbf81c525f6ed3cc7474a5451bdb48d308dea8227c229d1e8f4c1ca3)
-            mstore(VK_GATE_SETUP_6_Y_SLOT, 0x0a88d11473a3727652261721595b61340af2d5775bd6c77b105f6a2b8e2289a8)
+            mstore(VK_GATE_SETUP_6_X_SLOT, 0x1a39c9d70c2963af4158c3d178ad1752e51f2593311047248af5251e7d62980c)
+            mstore(VK_GATE_SETUP_6_Y_SLOT, 0x1409d3887aec4c70fbe6887fbd45082c3f368886f6ffa5bf7b347518d21fe5e1)
             mstore(VK_GATE_SETUP_7_X_SLOT, 0x1affeacdceb1b6f018bbb6762bb5aebcd54c3e24801ce01d06146d1bf0938b2c)
             mstore(VK_GATE_SETUP_7_Y_SLOT, 0x19a277dd9eaac40538f3177e3390ec21d097f88afb04158dd59e963a327fe9da)
 
diff --git a/l1-contracts/test/foundry/l1/da-contracts-imports/BlobsL1DAValidatorZKsyncOS.sol b/l1-contracts/test/foundry/l1/da-contracts-imports/BlobsL1DAValidatorZKsyncOS.sol
index 0228d5bd8..3ac41c6a9 100644
--- a/l1-contracts/test/foundry/l1/da-contracts-imports/BlobsL1DAValidatorZKsyncOS.sol
+++ b/l1-contracts/test/foundry/l1/da-contracts-imports/BlobsL1DAValidatorZKsyncOS.sol
@@ -1,12 +1,5 @@
 // SPDX-License-Identifier: MIT
 pragma solidity ^0.8.21;
 
-// This provides an interface for `da-contracts/contracts/BlobsL1DAValidatorZKsyncOS.sol:BlobsL1DAValidatorZKsyncOS`.
-// We can not import the file directly due to issues during imports from folders outside of the project.
-interface BlobsL1DAValidatorZKsyncOS {
-    /// @notice The published blob versioned hashes.
-    function publishedBlobs(bytes32 _versionedHash) external view returns (uint256 blockOfPublishing);
-
-    /// @notice Publishes all the blobs provided with a transaction.
-    function publishBlobs() external;
-}
+/// @dev Historical import path for tests. The Syscoin OS blob DA validator no longer exposes `publishBlobs` /
+/// `publishedBlobs` (non–EIP-4844 L1); `Executor` interacts only via `IL1DAValidator.checkDA`.
diff --git a/l1-contracts/test/foundry/l1/da-contracts-imports/DAContractsErrors.sol b/l1-contracts/test/foundry/l1/da-contracts-imports/DAContractsErrors.sol
index 5bb1483e9..07cfdedfa 100644
--- a/l1-contracts/test/foundry/l1/da-contracts-imports/DAContractsErrors.sol
+++ b/l1-contracts/test/foundry/l1/da-contracts-imports/DAContractsErrors.sol
@@ -54,3 +54,6 @@ error BlobNotPublished();
 
 // 0x8b003a9d
 error InvalidBlobsPublished(bytes32 publishedHash, bytes32 expectedHash);
+
+error BitcoinDAPrecompileCallFailed();
+error BitcoinDAVerificationFailed();
diff --git a/l1-contracts/test/foundry/l1/unit/concrete/BatchProcessing/CommittingZKsyncOS.t.sol b/l1-contracts/test/foundry/l1/unit/concrete/BatchProcessing/CommittingZKsyncOS.t.sol
index c621e20e7..c45a7d0e4 100644
--- a/l1-contracts/test/foundry/l1/unit/concrete/BatchProcessing/CommittingZKsyncOS.t.sol
+++ b/l1-contracts/test/foundry/l1/unit/concrete/BatchProcessing/CommittingZKsyncOS.t.sol
@@ -2,22 +2,42 @@
 pragma solidity 0.8.28;
 
 import "forge-std/console.sol";
-
+import {Vm} from "forge-std/Test.sol";
 import {Utils} from "../Utils/Utils.sol";
 import {ExecutorTest} from "./_Executor_Shared.t.sol";
 
+import {IExecutor} from "contracts/state-transition/chain-interfaces/IExecutor.sol";
 import {CommitBatchInfoZKsyncOS} from "contracts/state-transition/chain-interfaces/ICommitter.sol";
 import {L2DACommitmentScheme} from "contracts/common/Config.sol";
 import {MismatchL2DACommitmentScheme} from "contracts/state-transition/L1StateTransitionErrors.sol";
 import {ValidiumL1DAValidator} from "contracts/state-transition/data-availability/ValidiumL1DAValidator.sol";
 import {
+    BitcoinDAPrecompileCallFailed,
     InvalidPubdataHash,
-    InvalidBlobsPublished,
-    BlobNotPublished
+    InvalidBlobsPublished
 } from "../../../da-contracts-imports/DAContractsErrors.sol";
-import {BlobsL1DAValidatorZKsyncOS} from "../../../da-contracts-imports/BlobsL1DAValidatorZKsyncOS.sol";
+
+/// @dev Etched at Syscoin Bitcoin DA precompile address for Foundry tests.
+contract BitcoinDAMockOk {
+    fallback() external {
+        assembly {
+            mstore(0, 1)
+            return(0, 32)
+        }
+    }
+}
+
+contract BitcoinDAMockFail {
+    fallback() external {
+        revert();
+    }
+}
 
 contract CommittingTest is ExecutorTest {
+    // SYSCOIN: mirror the final-L1 compact edge DA ref domain used by `CommitterFacet`.
+    bytes internal constant SYSCOIN_EDGE_DA_REFS_DOMAIN = "SYSCOIN_EDGE_DA_REFS_V1";
+    uint256 internal constant SYSCOIN_EDGE_DA_REF_VERSION = 1;
+
     function isZKsyncOS() internal pure override returns (bool) {
         return true;
     }
@@ -68,12 +88,13 @@ contract CommittingTest is ExecutorTest {
     }
 
     function test_SuccessfullyCommitBatchWithBlobs() public {
+        vm.etch(address(uint160(0x63)), address(new BitcoinDAMockOk()).code);
+
         bytes32[] memory blobVersionedHashes = new bytes32[](2);
         blobVersionedHashes[0] = 0x01c024b4740620a5849f95930cefe298933bdf588123ea897cdf0f2462f6d2d5;
         blobVersionedHashes[1] = 0x01c024b4740620a5849f95930cefe298933bdf588123ea897cdf0f2462f6d2d6;
 
-        // 2 blobs, which we didn't prepublish
-        bytes memory operatorDAInput = abi.encodePacked(bytes32(0), bytes32(0));
+        bytes memory operatorDAInput = abi.encodePacked(blobVersionedHashes[0], blobVersionedHashes[1]);
 
         bytes32 daCommitment = keccak256(
             abi.encodePacked(
@@ -100,16 +121,16 @@ contract CommittingTest is ExecutorTest {
         admin.setDAValidatorPair(blobsl1DaValidatorZKsyncOS, L2DACommitmentScheme.BLOBS_ZKSYNC_OS);
 
         vm.prank(validator);
-        vm.blobhashes(blobVersionedHashes);
         committer.commitBatchesSharedBridge(address(0), commitBatchFrom, commitBatchTo, commitData);
     }
 
     function test_SuccessfullyCommitBatchWithBlobsPrepublished() public {
+        vm.etch(address(uint160(0x63)), address(new BitcoinDAMockOk()).code);
+
         bytes32[] memory blobVersionedHashes = new bytes32[](2);
         blobVersionedHashes[0] = 0x01c024b4740620a5849f95930cefe298933bdf588123ea897cdf0f2462f6d2d5;
         blobVersionedHashes[1] = 0x01c024b4740620a5849f95930cefe298933bdf588123ea897cdf0f2462f6d2d6;
 
-        // 2 prepublished blobs
         bytes memory operatorDAInput = abi.encodePacked(blobVersionedHashes[0], blobVersionedHashes[1]);
 
         bytes32 daCommitment = keccak256(
@@ -136,11 +157,7 @@ contract CommittingTest is ExecutorTest {
         vm.prank(address(owner));
         admin.setDAValidatorPair(blobsl1DaValidatorZKsyncOS, L2DACommitmentScheme.BLOBS_ZKSYNC_OS);
 
-        vm.blobhashes(blobVersionedHashes);
-        BlobsL1DAValidatorZKsyncOS(blobsl1DaValidatorZKsyncOS).publishBlobs();
-
         vm.prank(validator);
-        vm.blobhashes(new bytes32[](0));
         committer.commitBatchesSharedBridge(address(0), commitBatchFrom, commitBatchTo, commitData);
     }
 
@@ -172,13 +189,110 @@ contract CommittingTest is ExecutorTest {
         committer.commitBatchesSharedBridge(address(0), commitBatchFrom, commitBatchTo, commitData);
     }
 
+    function _edgeDARefsInput(
+        uint256 _edgeChainId,
+        uint256 _edgeBatchNumber,
+        bytes32 _blobVersionHash
+    ) internal pure returns (bytes memory input, bytes32 root) {
+        // SYSCOIN: canonical single-message opening for the Gateway-proven edge DA refs root.
+        bytes memory blobHashes = abi.encodePacked(_blobVersionHash);
+        bytes32 daCommitment = keccak256(blobHashes);
+        input = abi.encode(SYSCOIN_EDGE_DA_REF_VERSION, _edgeChainId, _edgeBatchNumber, daCommitment, blobHashes);
+        bytes32 refHash = keccak256(
+            abi.encodePacked(
+                SYSCOIN_EDGE_DA_REFS_DOMAIN,
+                _edgeChainId,
+                _edgeBatchNumber,
+                daCommitment,
+                blobHashes.length / 32,
+                blobHashes
+            )
+        );
+        root = keccak256(abi.encodePacked(bytes32(0), refHash));
+    }
+
+    function _commitValidiumBatchWithEdgeDARefs(
+        bytes memory _edgeDARefsInput,
+        bytes32 _edgeDARefsRoot,
+        bytes memory _expectedRevertData
+    ) internal {
+        // SYSCOIN: use Validium here so the test isolates final-L1 edge ref binding from Gateway's own DA validator.
+        bytes memory operatorDAInput = abi.encodePacked(bytes32(0));
+
+        CommitBatchInfoZKsyncOS memory correctNewCommitBatchInfo = newCommitBatchInfoZKsyncOS;
+        correctNewCommitBatchInfo.operatorDAInput = operatorDAInput;
+        correctNewCommitBatchInfo.daCommitment = bytes32(0);
+        correctNewCommitBatchInfo.daCommitmentScheme = L2DACommitmentScheme.EMPTY_NO_DA;
+        correctNewCommitBatchInfo.edgeDARefsInput = _edgeDARefsInput;
+        correctNewCommitBatchInfo.edgeDARefsRoot = _edgeDARefsRoot;
+
+        CommitBatchInfoZKsyncOS[] memory correctCommitBatchInfoArray = new CommitBatchInfoZKsyncOS[](1);
+        correctCommitBatchInfoArray[0] = correctNewCommitBatchInfo;
+
+        (uint256 commitBatchFrom, uint256 commitBatchTo, bytes memory commitData) = Utils
+            .encodeCommitBatchesDataZKsyncOS(genesisStoredBatchInfo, correctCommitBatchInfoArray);
+
+        address validiumL1DAValidator = address(new ValidiumL1DAValidator());
+        vm.prank(address(owner));
+        admin.setDAValidatorPair(validiumL1DAValidator, L2DACommitmentScheme.EMPTY_NO_DA);
+
+        vm.prank(validator);
+        if (_expectedRevertData.length != 0) {
+            vm.expectRevert(_expectedRevertData);
+        }
+        committer.commitBatchesSharedBridge(address(0), commitBatchFrom, commitBatchTo, commitData);
+    }
+
+    function test_RevertWhen_CommittingWithMalformedEdgeDARefsInput() public {
+        _commitValidiumBatchWithEdgeDARefs(
+            hex"1234",
+            bytes32(0),
+            abi.encodeWithSignature("SyscoinEdgeDARefInputMalformed(uint256)", 0)
+        );
+    }
+
+    function test_RevertWhen_CommittingWithEmptyEdgeDARefsInputAndNonzeroRoot() public {
+        (, bytes32 expectedRoot) = _edgeDARefsInput(l2ChainId, 1, keccak256("edge-blob"));
+
+        _commitValidiumBatchWithEdgeDARefs(
+            "",
+            expectedRoot,
+            abi.encodeWithSignature("SyscoinEdgeDARefsRootMismatch(bytes32,bytes32)", expectedRoot, bytes32(0))
+        );
+    }
+
+    function test_RevertWhen_CommittingWithWrongEdgeDARefsRoot() public {
+        vm.etch(address(uint160(0x63)), address(new BitcoinDAMockOk()).code);
+        (bytes memory edgeDARefsInput, bytes32 actualRoot) = _edgeDARefsInput(l2ChainId, 1, keccak256("edge-blob"));
+        bytes32 wrongRoot = keccak256("wrong-root");
+
+        _commitValidiumBatchWithEdgeDARefs(
+            edgeDARefsInput,
+            wrongRoot,
+            abi.encodeWithSignature("SyscoinEdgeDARefsRootMismatch(bytes32,bytes32)", wrongRoot, actualRoot)
+        );
+    }
+
+    function test_RevertWhen_CommittingWithSwappedEdgeDARefsInput() public {
+        vm.etch(address(uint160(0x63)), address(new BitcoinDAMockOk()).code);
+        (, bytes32 expectedRoot) = _edgeDARefsInput(l2ChainId, 1, keccak256("edge-blob-a"));
+        (bytes memory swappedInput, bytes32 actualRoot) = _edgeDARefsInput(l2ChainId, 1, keccak256("edge-blob-b"));
+
+        _commitValidiumBatchWithEdgeDARefs(
+            swappedInput,
+            expectedRoot,
+            abi.encodeWithSignature("SyscoinEdgeDARefsRootMismatch(bytes32,bytes32)", expectedRoot, actualRoot)
+        );
+    }
+
     function test_RevertWhen_CommittingWithWrongL2DACommitmentScheme() public {
+        vm.etch(address(uint160(0x63)), address(new BitcoinDAMockOk()).code);
+
         bytes32[] memory blobVersionedHashes = new bytes32[](2);
         blobVersionedHashes[0] = 0x01c024b4740620a5849f95930cefe298933bdf588123ea897cdf0f2462f6d2d5;
         blobVersionedHashes[1] = 0x01c024b4740620a5849f95930cefe298933bdf588123ea897cdf0f2462f6d2d6;
 
-        // 2 blobs, which we didn't prepublish
-        bytes memory operatorDAInput = abi.encodePacked(bytes32(0), bytes32(0));
+        bytes memory operatorDAInput = abi.encodePacked(blobVersionedHashes[0], blobVersionedHashes[1]);
 
         bytes32 daCommitment = keccak256(
             abi.encodePacked(
@@ -205,7 +319,6 @@ contract CommittingTest is ExecutorTest {
         admin.setDAValidatorPair(blobsl1DaValidatorZKsyncOS, L2DACommitmentScheme.BLOBS_ZKSYNC_OS);
 
         vm.prank(validator);
-        vm.blobhashes(blobVersionedHashes);
         vm.expectRevert(abi.encodeWithSelector(MismatchL2DACommitmentScheme.selector, 3, 4));
         committer.commitBatchesSharedBridge(address(0), commitBatchFrom, commitBatchTo, commitData);
     }
@@ -255,12 +368,13 @@ contract CommittingTest is ExecutorTest {
     }
 
     function test_RevertWhen_CommittingWithWrongBlobs() public {
+        vm.etch(address(uint160(0x63)), address(new BitcoinDAMockOk()).code);
+
         bytes32[] memory blobVersionedHashes = new bytes32[](2);
         blobVersionedHashes[0] = 0x01c024b4740620a5849f95930cefe298933bdf588123ea897cdf0f2462f6d2d5;
         blobVersionedHashes[1] = 0x01c024b4740620a5849f95930cefe298933bdf588123ea897cdf0f2462f6d2d7;
 
-        // 2 blobs, which we didn't prepublish
-        bytes memory operatorDAInput = abi.encodePacked(bytes32(0), bytes32(0));
+        bytes memory operatorDAInput = abi.encodePacked(blobVersionedHashes[0], blobVersionedHashes[1]);
 
         bytes32 daCommitment = keccak256(
             abi.encodePacked(
@@ -287,7 +401,6 @@ contract CommittingTest is ExecutorTest {
         admin.setDAValidatorPair(blobsl1DaValidatorZKsyncOS, L2DACommitmentScheme.BLOBS_ZKSYNC_OS);
 
         vm.prank(validator);
-        vm.blobhashes(blobVersionedHashes);
         vm.expectRevert(
             abi.encodeWithSelector(
                 InvalidBlobsPublished.selector,
@@ -298,12 +411,13 @@ contract CommittingTest is ExecutorTest {
         committer.commitBatchesSharedBridge(address(0), commitBatchFrom, commitBatchTo, commitData);
     }
 
-    function test_RevertWhen_CommittingWithNotPrepublishedBlobs() public {
+    function test_RevertWhen_CommittingWithBlobsBitcoinDAFails() public {
+        vm.etch(address(uint160(0x63)), address(new BitcoinDAMockFail()).code);
+
         bytes32[] memory blobVersionedHashes = new bytes32[](2);
         blobVersionedHashes[0] = 0x01c024b4740620a5849f95930cefe298933bdf588123ea897cdf0f2462f6d2d5;
         blobVersionedHashes[1] = 0x01c024b4740620a5849f95930cefe298933bdf588123ea897cdf0f2462f6d2d6;
 
-        // 2 prepublished blobs
         bytes memory operatorDAInput = abi.encodePacked(blobVersionedHashes[0], blobVersionedHashes[1]);
 
         bytes32 daCommitment = keccak256(
@@ -331,7 +445,7 @@ contract CommittingTest is ExecutorTest {
         admin.setDAValidatorPair(blobsl1DaValidatorZKsyncOS, L2DACommitmentScheme.BLOBS_ZKSYNC_OS);
 
         vm.prank(validator);
-        vm.expectRevert(BlobNotPublished.selector);
+        vm.expectRevert(BitcoinDAPrecompileCallFailed.selector);
         committer.commitBatchesSharedBridge(address(0), commitBatchFrom, commitBatchTo, commitData);
     }
 
diff --git a/l1-contracts/test/foundry/l1/unit/concrete/BatchProcessing/_Executor_Shared.t.sol b/l1-contracts/test/foundry/l1/unit/concrete/BatchProcessing/_Executor_Shared.t.sol
index 57e1a62a0..10c5cc507 100644
--- a/l1-contracts/test/foundry/l1/unit/concrete/BatchProcessing/_Executor_Shared.t.sol
+++ b/l1-contracts/test/foundry/l1/unit/concrete/BatchProcessing/_Executor_Shared.t.sol
@@ -428,6 +428,9 @@ contract ExecutorTest is UtilsCallMockerTest {
             lastBlockNumber: uint64(2),
             chainId: l2ChainId,
             operatorDAInput: "",
+            // SYSCOIN: default tests have no compact edge DA refs unless explicitly set.
+            edgeDARefsInput: "",
+            edgeDARefsRoot: bytes32(0),
             slChainId: block.chainid
         });
 
diff --git a/tools/verifier-gen/data/ZKsyncOS_plonk_scheduler_key.json b/tools/verifier-gen/data/ZKsyncOS_plonk_scheduler_key.json
index d442c78ed..364326243 100644
--- a/tools/verifier-gen/data/ZKsyncOS_plonk_scheduler_key.json
+++ b/tools/verifier-gen/data/ZKsyncOS_plonk_scheduler_key.json
@@ -6,16 +6,16 @@
   "gate_setup_commitments": [
     {
       "x": [
-        2412641821283476826,
-        14471423996490288158,
-        5225950598292101952,
-        1228487007723799555
+        3773395308681003173,
+        13183838396445194906,
+        11713485027438163200,
+        2971639777893998836
       ],
       "y": [
-        8520728407555862345,
-        11212192803778776518,
-        1988917732523207549,
-        227643856272740304
+        3670602382172319028,
+        14506365593086289802,
+        9947551342584659057,
+        2146535903814228849
       ],
       "infinity": false
     },
@@ -96,16 +96,16 @@
     },
     {
       "x": [
-        8944884564489936035,
-        2007278130147797026,
-        6876666556121916741,
-        2048660560198245458
+        10012950159411025932,
+        16509956073077098276,
+        4708728714856044370,
+        1889763444117824431
       ],
       "y": [
-        1179778362695125416,
-        788927593361885051,
-        5919444192225616180,
-        759086422981571190
+        8877849514919060961,
+        4554978186382779839,
+        18151345430381004844,
+        1443917738680274032
       ],
       "infinity": false
     },
